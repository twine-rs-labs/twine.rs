# D4/D5 Audit Artifact — 2026-06-21

This artifact records the D4 Text mode visual check and the D5 graph/reveal
implementation check performed after the D1-D5/M0-M6 audit cleanup.

## D4 Text Mode Screenshot Pair

- Reference artifact:
  [`d4-textmode-design-2026-06-21.png`](./d4-textmode-design-2026-06-21.png)
  captures `docs/design-system/ui_kits/workbench/index.html` after selecting
  the `Text` tab.
- Running app:
  [`d4-textmode-app-2026-06-21.png`](./d4-textmode-app-2026-06-21.png)
  captures `/#/stories/story-d4-audit` with seeded local-storage story data in
  Text mode.

The app capture verifies the D4 structure now present in the live route:
AppShell top chrome, Text mode source editor, passage navigator, right inspector,
bottom references drawer, DS badges/buttons/icons, and the explicit
`Reveal in Graph` affordance.

## D5 Implementation Check

- `StoryGraphPanel` renders DS `PassageNode`s from `queryGraphProjection()`.
- Viewport/focus options are passed through the query boundary.
- Generated layout save flows through the host command path.
- Migrated editor controls for passage create/delete/move/rename/start,
  story rename, story format, snap-to-grid, zoom, text, script, stylesheet, and
  asset operations flow through `CoreProjectHost.applyStoryCommand()`.
- Play/Test preview routes no longer reload on story-format load-state churn, and
  in-flight story-format property loads dedupe by format ID.
- Explicit reveal-in-graph is wired from Text mode, search selection, and
  diagnostics. When invoked from Text mode, the workspace switches to Split so
  the selected passage is visible in the graph while the source context remains
  available.

## Remaining Non-D5 Work

- 10k/50k graph performance is not yet certified; SVG edge density remains the
  main scale risk.
- Native/WASM `ProjectSession` bridging remains a core follow-up.
- M0/M5 file-backed project and asset inventory integration remains partial in
  the React/Electron app and was tracked in
  [`milestones-and-enhancement-catalogue.md`](../research/milestones-and-enhancement-catalogue.md).
