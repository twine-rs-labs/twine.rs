A checkbox supporting checked and indeterminate (mixed) states.

```jsx
<Checkbox checked={matchCase} onChange={setMatchCase} label="Match Case" />
<Checkbox checked={false} indeterminate label="3 of 8 selected" />
```

Use `indeterminate` for partial bulk-selection headers.
