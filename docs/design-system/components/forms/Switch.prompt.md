An on/off toggle for boolean settings.

```jsx
<Switch checked={snap} onChange={setSnap} label="Snap to grid" />
<Switch checked={reducedMotion} onChange={setReducedMotion} label="Reduced motion" />
```

The "on" track uses the accent blue. Provide `label` for an inline caption.
