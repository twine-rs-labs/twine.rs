# Codebase audit — 2026-07-21

Status: **complete**<br>
Owner: repository maintainers<br>
Last verified: 2026-07-29<br>
Source of truth: revision `94eaa0031` plus the verification results recorded below<br>
Started: 2026-07-21 18:57 CEST<br>
Ended: 2026-07-21 23:40 CEST (user-requested pause excluded and overrun authorized)<br>
Audited revision: `94eaa0031` (`main`, detached worktree)<br>
Audit scope: correctness, security, tests, documentation consistency, build and packaging, and release readiness.

This report is updated incrementally. Findings are only promoted to the main table when they have direct repository evidence or a reproducible check. Severity describes likely user/release impact: critical, blocker, high, medium, low, or informational.

## Executive summary

This audit originally confirmed **60 findings**: 5 critical, 1 blocker, 17 high, 30 medium, and 7 low. As of 2026-07-29, **58 findings are marked fixed**: BUG-001, BUG-002, BUG-003, BUG-004, BUG-005, BUG-006, BUG-007, BUG-008, BUG-009, BUG-010, BUG-011, BUG-012, BUG-013, BUG-014, BUG-015, BUG-016, BUG-017, BUG-018, BUG-019, BUG-020, BUG-021, BUG-022, BUG-023, DOC-001, DOC-002, DOC-003, DOC-004, DOC-005, DOC-006, DOC-007, DOC-008, DOC-009, DOC-010, DOC-011, DOC-012, DEP-002, SEC-001, SEC-002, SEC-003, SEC-004, SEC-005, SEC-006, SEC-007, REL-001, REL-002, REL-003, REL-004, REL-005, REL-006, REL-007, REL-008, REL-009, REL-010, REL-012, REL-013, REL-014, REL-015, and REL-016. REL-011 now has repository controls but remains open until the first intentional release supplies a protected tag, immutable artifact set, and completed checklist. The current revision should still **not** be released because the release checklist remains unexercised and DEP-001 remains open.

The broad unit, browser E2E, Rust, reproducible web-build, host-build, mdBook, formatting, Clippy, and focused host packaged-app checks pass. Dedicated quality CI now enforces the JavaScript, documentation, minimum/current Rust, and repeatable web-archive contracts with immutable action revisions and Node 24.18.0. Target-native jobs build, retain, and exercise every supported installable format before a clearly labeled seven-artifact local test bundle is retained; distributable unsigned and signed profiles use separate fail-closed assembly paths. Non-host jobs still require their first CI execution. Dependency review now finds one high-severity npm advisory group in development/release tooling but no production npm advisory; the locked Rust graph passes RustSec audit and a dedicated CI gate covers lockfile changes plus weekly advisory refreshes. The current macOS ARM package is architecture-consistent, contains the deny-by-default permission policy, and omits unsupported sensitive-device usage declarations; as a local-profile artifact it is deliberately ad-hoc signed and unnotarized.

## Confirmed findings

| ID              | Severity | Area                | Finding                                                                                              | Evidence and impact                                                                                                                                                                                                             | Recommendation                                                                                                              |
| --------------- | -------- | ------------------- | ---------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| DOC-001 (fixed) | Medium   | Setup documentation | The documented Node/npm prerequisites are weaker than the versions enforced by the package manifest. | `README.md` now names Node 24.18.0, npm 11+, and the checked-in Rust 1.96.0 toolchain definition. `.nvmrc`, `package.json`, active GitHub Actions workflows, and contributor prerequisites agree on the supported Node 24 line. | Implemented; update the README, `.nvmrc`, manifest engines, and workflow pin together when the supported toolchain changes. |

### Correctness and data-integrity findings

| ID              | Severity | Finding                                                                                                                     | Evidence and impact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Recommendation                                                                                                                                                                                                                       |
| --------------- | -------- | --------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| BUG-001 (fixed) | High     | Pending debounced legacy-story saves are not guaranteed to finish before process exit.                                      | Story HTML saves are delayed up to one second (`src/electron/main-process/ipc.ts:546-579`). The `will-quit` listener calls and awaits each debounce `flush()` (`:589-602`), but Electron/Node event emitters do not await a listener's returned promise and the handler does not call `event.preventDefault()`. The process may exit while the latest trailing write is in flight. The unit test at `ipc.test.ts:943-966` manually awaits the listener, so it does not model real shutdown.                                                                                                                                                                                                                                                                   | Intercept `before-quit`/`will-quit`, prevent the first quit, await a single idempotent flush barrier, handle failures, then explicitly resume quit; cover it with a process-level shutdown test.                                     |
| BUG-002 (fixed) | High     | Several main-process IPC write failures become unhandled promises with no reliable renderer acknowledgement.                | Async `ipcMain.on` handlers for delete, rename, JSON save, and debounced story save catch errors, show a dialog, then rethrow (`src/electron/main-process/ipc.ts:452-463,521-587`); event emitters ignore returned promises. Scratch preview listeners call promise-returning functions without awaiting or catching them (`:498-510`). Disk-full, permission, or invalid-path errors can therefore become unhandled main-process rejections (potentially terminating the process depending runtime policy), while renderer save state receives no structured failure.                                                                                                                                                                                        | Use `ipcMain.handle`/`ipcRenderer.invoke` for asynchronous operations or attach terminal catches; return typed success/failure acknowledgements and make renderer save state react to them.                                          |
| BUG-003 (fixed) | Medium   | Persistence repair can durably write untranslated key names because i18n initialization races state loading.                | `src/util/i18n.ts:9-24` starts async initialization and drops its promise; `src/index.tsx:4,15-19` renders immediately. `StateLoader` runs outside route Suspense and repairs persisted data without awaiting i18n (`src/app.tsx:23-30`, `src/store/state-loader.tsx:77-106,136-154`). Load/repair calls `storyDefaults()`/`passageDefaults()`, whose names come from `i18n.t` (`src/store/stories/defaults.ts:4-30`). For an older/partial record with a missing/invalid name, the repair can persist `store.storyDefaults.name` or its passage equivalent. All three browser engines emitted the corresponding initialization warning during the audit.                                                                                                     | Await i18n initialization before rendering/state repair, or make storage defaults locale-independent and localize only at presentation time; add a migration test with missing names and delayed locale loading.                     |
| BUG-004 (fixed) | Medium   | “Start Empty Here” and “Use Existing Stories Here” perform the same action.                                                 | The change-library dialog exposes both choices (`src/electron/main-process/story-directory.ts:255-268`), but only response 0 has special handling (`:274-280`). Responses 1 and 2 both leave destination contents untouched, set the same preference, and relaunch, so a non-empty folder selected with “Start Empty Here” loads its existing stories. Tests cover response 1 but not response 2 (`story-directory.test.ts:288-296`).                                                                                                                                                                                                                                                                                                                         | Give “Start Empty” distinct, safe semantics (for example require/choose a new empty subdirectory) or remove the duplicate option; add non-empty destination tests for every response.                                                |
| BUG-005 (fixed) | Medium   | Story repair checks the story ID when deciding whether to regenerate an empty IFID.                                         | `src/store/stories/reducer/repair/repair-story.ts:112-119` repairs `story.ifid` when it is invalid or when `story.id === ''`; the second condition should inspect `story.ifid`. Consequently an empty IFID can survive repair when the story ID is valid, while an empty story ID needlessly replaces an otherwise valid IFID. That can leave malformed story identity data or unexpectedly change identity during migration.                                                                                                                                                                                                                                                                                                                                 | Change the condition to `story.ifid === ''` and add regression cases for an empty IFID with a valid ID and an empty ID with a valid IFID.                                                                                            |
| BUG-006 (fixed) | Critical | Creating a project can silently replace another project whose name normalizes to the same folder slug.                      | `pathSlug()` collapses punctuation and case (`src/electron/main-process/project-folder.ts:567-588`), and `projectRootForStory()` plus `createProjectFolder()` use the resulting path without an existence/collision check (`:2237-2244,5004-5026`). The UI rejects only equal story names (`src/routes/new-project/new-project-route.tsx:454-470`). On an existing root, the native store swaps in the new project and deletes the retired tree without a backup (`crates/twine_store/src/lib.rs:414-481`). The audit reproduced `A:B` followed by `A?B`: both selected `a-b.twine.rs`, and only the second story remained.                                                                                                                                   | Allocate collision-resistant roots (for example slug plus stable ID), refuse implicit overwrite, and require an explicit, backed-up replace flow; add punctuation/case/truncation collision tests through the UI and native backend. |
| BUG-007 (fixed) | Critical | A full save of one story deletes every sibling story from a multi-story project folder.                                     | The native save accepts one story and constructs `Project::from_story`, whose `stories` is a one-element vector (`crates/twine_native/src/lib.rs:661-721`; `crates/twine_model/src/lib.rs:755-772`). It loads an existing project only to recover source layout, then atomically replaces the entire root with backups disabled. `saveProjectFolder()` falls back to this full-save path whenever an incremental save is unavailable (`src/electron/main-process/project-folder.ts:5029-5059,5147-5175,5664-5687`). In a temporary two-story project, saving story one changed `storyIds` from `["one","two"]` to `["one"]` and physically removed story two's passage.                                                                                       | Make full save load and merge the existing project by story ID, preserve all sibling content and metadata, enable a recoverable backup, and add native plus packaged-app multi-story save/rename tests.                              |
| BUG-008 (fixed) | High     | Passage layout is keyed globally by passage ID, allowing stories in the same project to overwrite each other's coordinates. | `Project.layout.passages` is a single `BTreeMap<PassageId, ...>` although a project contains multiple stories (`crates/twine_model/src/lib.rs:602-612,744-752`). New passage IDs are unique only within one story (`crates/twine_core/src/lib.rs:9155-9170`), while file-backed stories sharing a root intentionally share one core session (`src/core/project-host.tsx:2084-2091,2558-2564`). Moving a passage inserts by bare passage ID (`crates/twine_core/src/lib.rs:3617-3635`), and graph projection prefers that global entry (`crates/twine_graph/src/lib.rs:729-735`). Deletion also leaves the layout entry behind (`crates/twine_core/src/lib.rs:6273-6317`), so cross-story collisions and delete/recreate can apply persistent wrong positions. | Scope layout keys by story ID and passage ID (with a migration), remove layout on deletion, and test duplicate IDs across stories plus delete/recreate behavior.                                                                     |
| BUG-009 (fixed) | High     | Duplicating or importing a nonempty story after startup can crash `StateLoader`.                                            | `StateLoader` rejects stories whose passages lack text (`src/store/state-loader.tsx:15-27`), then intentionally separates every loaded story into metadata-only state after startup (`:156-179`). Duplicate materializes and inserts a complete copy (`src/routes/story-list/story-list-route.tsx:260-264`; `src/store/stories/action-creators/duplicate-story.ts:13-38`), causing the effect to rerun and validate the metadata-only original alongside the complete copy. It then throws “Loaded story state is missing passage documents.” Importing a complete story into an already separated state has the same shape.                                                                                                                                  | Make separation a one-time hydration boundary or make later mutations document-aware without revalidating metadata-only stories; add post-hydration duplicate and import component tests.                                            |
| BUG-010 (fixed) | Medium   | Concurrent conflict reviews for different project roots can hide one review and leave its queue permanently paused.         | Multiple roots have concurrent queues (`src/store/project-session-sync.tsx:45-65`; `src/store/native-project-delta-queue.ts:26-40,58-80`), but the UI stores only one `pendingReview` and every conflict/recovery replaces it (`project-session-sync.tsx:75-84,152-195`). Resolution resumes only the displayed root (`:356-359,394-396,418-420`). If two roots conflict before review, the second hides the first; resolving it leaves the first paused with no remaining UI path until restart.                                                                                                                                                                                                                                                             | Maintain a keyed/FIFO review queue, resume or cancel every root deterministically, and test two simultaneous root conflicts and recovery conflicts.                                                                                  |
| BUG-011 (fixed) | Medium   | Clearing a native delta queue does not cancel an in-flight drain and can corrupt a replacement queue for the same root.     | `clearRoot()` only deletes the map entry (`src/store/native-project-delta-queue.ts:54-56`). The active `drain()` retains the old queue across awaits and continues processing (`:58-80`); its `finally` can delete a newly created map entry for the same root. Session cleanup calls this just before stopping sessions (`src/store/project-session-sync.tsx:288-294`). Queued work may therefore run after shutdown, old/new drains may overlap, and the old drain may orphan the new queue.                                                                                                                                                                                                                                                                | Add queue generations or cancellation tokens, make cleanup await drain termination, and cover clear-during-flight plus clear/re-enqueue races.                                                                                       |
| BUG-012 (fixed) | Medium   | Unicode search results navigate to the wrong editor offset.                                                                 | Rust regex and fuzzy search expose UTF-8 byte offsets (`crates/twine_core/src/lib.rs:9618-9658,9681-9728,9767-9797`). The renderer passes `hit.start` through search navigation (`src/dialogs/story-search.tsx:169-191`) to CodeMirror, which expects a JavaScript UTF-16 offset (`src/components/control/source-editor/source-editor.tsx:1355-1375`). For `😀 alpha`, Rust reports byte offset 5 for `alpha`, while CodeMirror expects UTF-16 offset 3.                                                                                                                                                                                                                                                                                                      | Convert search ranges to UTF-16 at the Rust boundary, as asset references already do, and add emoji/accented-text navigation tests.                                                                                                  |
| BUG-013 (fixed) | Medium   | Rust text metrics count UTF-8 bytes as characters and omit a terminal empty line.                                           | Character counts use `str.len()` and line counts use `text.lines().count()` (`crates/twine_core/src/lib.rs:6852-6861,7495-7499,7874-7887,9605-9607`), while the renderer contract uses JavaScript UTF-16 `.length` and newline splitting (`src/core/story-index.ts:63-65,107-115`). Thus `é😀` is reported as 6 instead of 3 code units, and `a\n` as one rather than two editor lines, producing wrong Story Details and Contents metrics.                                                                                                                                                                                                                                                                                                                   | Define the UI metric contract explicitly, convert/count consistently, and add non-ASCII plus trailing-newline parity tests across Rust and TypeScript.                                                                               |
| BUG-014 (fixed) | Low      | Two partial-passage paths spread the `passageDefaults` function instead of invoking it.                                     | `passageDefaults` is a factory (`src/store/stories/defaults.ts:4-14`), but local-storage loading and `createStory` use `...passageDefaults` (`src/store/persistence/local-storage/stories/load.ts:91-97`; `src/store/stories/reducer/create-story.ts:34-41`). Function objects contribute no default fields. Startup repair masks many persisted cases, but post-startup partial creation can lack expected geometry, flags, names, tags, and text; the loader test leaves this as a todo.                                                                                                                                                                                                                                                                    | Invoke `passageDefaults()` at both sites, define the accepted partial shape, and turn the loader todo plus a post-startup creation case into tests.                                                                                  |
| BUG-015 (fixed) | Medium   | Failure during a global tag rename can undo an unrelated edit in another project.                                           | Global rename applies the command to every core host, records successful no-op hosts, and calls `host.undo()` on all recorded hosts if a later host fails (`src/core/project-host.tsx:2185-2200`). A host without the old tag creates no transaction (`crates/twine_core/src/lib.rs:3128-3153,6670-6701`), so rollback on that host pops its previous unrelated undo entry.                                                                                                                                                                                                                                                                                                                                                                                   | Return an explicit transaction token/change flag per host and roll back only transactions created by this operation; test a no-op early host with existing history followed by a failing host.                                       |

| BUG-016 (fixed) | High | Deleting a native project also attempts to delete an unmentioned same-named legacy HTML story. | The list flow trashes the `.twine.rs` root and dispatches story deletion (`src/routes/story-list/story-list-route.tsx:347-376`). Patch persistence then unconditionally calls the legacy `deleteStory` bridge (`src/store/persistence/electron-ipc/stories/save-middleware.ts:119-132`), which trashes `<story name>.html` (`src/electron/main-process/story-file.ts:318-329`). Coexisting same-IFID HTML is anticipated and deliberately hidden when a native project loads (`story-file.ts:223-231`). Project deletion can therefore remove both sources without disclosing the second target; if none exists, it can show a spurious deletion error. | Make deletion storage-kind aware, enumerate every target in confirmation, and test coexistence plus missing-legacy-file behavior. |
| BUG-017 (fixed) | High | A full native save deletes unmanaged files in the project root. | The native store writes a generated temporary project and preserves only the previous `assets/` tree (`crates/twine_store/src/lib.rs:431-436,698-713`), then swaps the root and deletes the retired tree (`:461-481`). A root-level `README.md`, `.gitignore`, build configuration, or other externally managed file therefore disappears on any full save, even though project folders are promoted for external editing and source control (`docs/en/src/story-library/location.md:50-56`). | Preserve unknown files by default or maintain a validated ownership manifest, warn on destructive conflicts, create a backup, and add round-trip tests with root-level VCS/config files. |
| BUG-018 (fixed) | Medium | Previews from different projects share a mutable asset namespace. | Every preview HTML file uses one scratch root (`src/electron/main-process/scratch-file.ts:276-287`). Link mode removes and relinks the same top-level `assets` directory to the latest project (`:232-252`); copy mode overwrites collisions but does not clear stale ordinary files (`:210-229`). Opening preview B can make still-open preview A serve B's assets, while A-only leftovers can mask missing assets in B. | Give each preview an isolated opaque directory, clean it on lifecycle completion, and test simultaneous projects with colliding and missing asset names. |
| BUG-019 (fixed) | Medium | macOS file-open requests received after renderer startup are queued but never consumed. | The main process queues every `open-file` event (`src/electron/main-process/index.ts:53-65`), but the renderer consumes queued requests only once in a mount-only effect (`src/routes/command-line-open-sync.tsx:38-53,149-152`). Opening a `.twine.rs` folder from Finder after that effect leaves the request queued indefinitely with no notification or polling path. | Push open requests over an event subscription after readiness while retaining startup queueing, acknowledge consumption, and test a second macOS open event after the window is ready. |
| BUG-020 (fixed) | Medium | Native root replacement has no rollback if installing the temporary project fails. | Packaged saves disable backups (`crates/twine_native/src/lib.rs:713-721`). The store renames the original root to a retired sibling and then renames the temporary root into place without a recovery guard (`crates/twine_store/src/lib.rs:461-481`). If the second rename fails, the expected project path disappears and the original remains stranded under a generated retired name. | Use a guarded transaction that restores the retired root on failure, retain a recoverable backup or journal, and inject rename failures in platform-specific tests. |

| BUG-021 (fixed) | Critical | Ordinary web story selection can silently erase persisted passage bodies. | Hydration strips `text` from store passages (`src/core/bootstrap-stories.ts:43-52`). Selecting a story dispatches a direct `updateStory` (`src/routes/story-list/story-list-route.tsx:310`; `src/store/stories/action-creators/select-story.ts:41-63`), and local-storage persistence serializes every metadata-only passage without filtering trivial changes or restoring documents (`src/store/persistence/local-storage/stories/save-middleware.ts:315-335`). In an isolated Chromium profile, selecting between two stories overwrote a 42-character body; after reload, repair had converted the missing field to `text: ""` and the library word count fell from 7 to 0. A subsequent New Passage action then raised “A complete project snapshot requires materialized documents.” | Keep document bodies authoritative and merge metadata changes into persisted records, exclude selection-only updates, and add a web hydrate/select/reload regression test with nonempty passages. |
| BUG-022 (fixed) | Critical | Passage IDs generated per story collide in the global browser-storage namespace. | Core-generated IDs are unique only within one story (`crates/twine_core/src/lib.rs:9155-9170`), while web stories use separate sessions (`src/core/project-host.tsx:2084-2092`). Browser persistence keys passages globally as `twine-passages-${passage.id}` and maintains one global ID list (`src/store/persistence/local-storage/stories/save.ts:22-31,69-78`). A fresh browser reproduction created two stories and used each graph's normal New action once: localStorage contained only one `passage-2`, owned by story two, and after reload the library reported one passage for story one versus two for story two. | Namespace passage storage by story and passage ID or guarantee project-wide IDs with a migration; add two-story create/save/delete/reload collision tests. |
| BUG-023 (fixed) | High | The local-storage transaction can leave mixed revisions and orphaned records when quota is exhausted. | `doUpdateTransaction()` mutates individual records before writing the indexes and has no staging or rollback (`src/store/persistence/local-storage/stories/save.ts:22-31,37-50,69-78`). Multi-record middleware operations issue several writes (`save-middleware.ts:106-162,216-222,318-335`). If a `setItem()` raises `QuotaExceededError`, earlier writes remain and indexes may stay old or partially update, while the reducer retains the new in-memory state after reporting the error (`src/store/stories/stories-context.tsx:117-123`). | Stage a complete version under revisioned keys, atomically switch one manifest key only after success, clean failed revisions, surface durable-save failure, and inject quota failures at every write boundary. |

### Security and dependency findings

| ID              | Severity | Finding                                                                                                           | Evidence and impact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Recommendation                                                                                                                                                                                                                                                                                                                      |
| --------------- | -------- | ----------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SEC-001 (fixed) | Critical | Story-format scripts execute beside a privileged Electron bridge that accepts renderer-supplied filesystem roots. | `src/electron/main-process/init-app.ts:38-42` disables `contextIsolation`. `src/electron/main-process/preload.ts:14-70` loads any user-entered/stored story-format URL as a page script and `:314-318` places the full `twineElectron` bridge on that same `window`; the add/load path has no scheme or host allowlist (`src/routes/story-formats/story-formats-route.tsx:282-290,465-479`). `ipc.ts:434-442` trusts a renderer-supplied project root, and the native save path accepts an arbitrary `PathBuf` without requiring an existing project manifest (`crates/twine_native/src/lib.rs:661-721`). A malicious/compromised format can invoke `saveProjectFolder` on a writable user directory; the store swaps the root and deletes the retired tree (`crates/twine_store/src/lib.rs:414-481`). This turns a format install, endpoint compromise, HTTP interception, or another renderer injection into arbitrary directory replacement/destruction. | Do not execute format code in the application renderer. Use a separate sandboxed origin/process with no app IPC, enable context isolation, validate sender/frame/origin on every IPC call, replace path arguments with opaque capabilities granted by trusted dialogs, and require/canonicalize a valid project root before writes. |
| SEC-002 (fixed) | High     | Scratch preview IPC permits filename traversal and arbitrary text-file overwrite.                                 | `preload.ts:206-210` accepts renderer-controlled data and filename; `ipc.ts:498-509` forwards both unchanged. `scratch-file.ts:83-88,276-287` joins the filename to the scratch directory and writes/opens it without a basename or containment check. A `../` filename escapes the scratch root and can overwrite any known writable path, then asks the OS to open it. Normal UI callers use UUID-based names, but SEC-001 gives loaded format code direct bridge access. Asset output paths have a containment validator; preview filenames do not.                                                                                                                                                                                                                                                                                                                                                                                                      | Generate opaque filenames in the main process or require a single safe basename, enforce canonical containment/no-follow semantics, cap payloads, and never accept renderer-supplied source paths for copying.                                                                                                                      |
| SEC-003 (fixed) | Medium   | External navigation forwards unvalidated URL schemes to the operating system.                                     | `src/electron/main-process/init-app.ts:77-92` blocks in-app navigation/popups but passes every supplied URL to `shell.openExternal` in the default `system` mode. Story-format HTML can supply links, and compromised renderer code can trigger navigation. `file:` and arbitrary custom schemes can invoke platform handlers; impact is platform/handler dependent. The update-response URL is likewise forwarded after confirmation (`check-for-update.ts:39-59`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Centralize URL validation, allow only `https:` plus explicitly justified schemes, and reject credentials, local files, script/data URLs, and arbitrary custom protocols before any OS handoff.                                                                                                                                      |
| DEP-001         | Medium   | The locked development/release dependency graph contains one high-severity URL-validation advisory.               | The 2026-07-23 `npm audit` reports only `fast-uri@3.1.3` (GHSA-v2hh-gcrm-f6hx), deduplicated through `ajv@8.20.0` under both Electron Builder and Workbox build tooling. The latest lock refresh fixed the earlier vulnerable `brace-expansion` and `shell-quote` instances. `fast-uri` remains development-only, so the shipped runtime dependency graph is unaffected, but crafted build or packaging inputs could confuse an allow/deny host boundary in contributor or CI tooling.                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Refresh the permitted transitive resolution to `fast-uri >=3.1.4`, rerun the full and production-only audits plus build/package checks, and add advisory checks to CI with an explicit policy for development-only findings.                                                                                                        |
| DEP-002 (fixed) | Medium   | The locked Rust graph fails RustSec audit with one vulnerability and one unsoundness warning.                     | `cargo-audit 0.22.2` against RustSec database commit `b5fc89b8` reports RUSTSEC-2026-0204 for `crossbeam-epoch@0.9.18`, pulled into shipped native/storage code through Rayon, and RUSTSEC-2026-0190 for `anyhow@1.0.102` in the CLI. Repository search found no direct use of the advisory-triggering pointer formatting or `Error::downcast_mut` APIs, reducing current reachability, but patched versions are available.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Refresh the lockfile to patched `crossbeam-epoch >=0.9.20` and `anyhow >=1.0.103`, rerun all Rust checks, and gate future lockfiles with RustSec policy in CI.                                                                                                                                                                      |

| SEC-004 (fixed) | Medium | Direct HTML and Twee imports had no file-size or parser-budget limit. | Browser and desktop entry points now reject direct sources above 50 MiB before `File.text()`, `FileReader`, native bridging, or fallback reads. Native and TypeScript readers use opened-handle metadata, bounded chunks, and a trailing-byte sentinel. Shared HTML/Twee guards bound source, story, passage, element/tag, nesting, graph-metadata, tag-color, and aggregate legacy-import work before or during parsing; deterministic header/tag parsing, outermost-story selection, bounded metadata indexing, and linear asset-reference rewriting remove the reviewed amplification paths. Boundary, parser-recovery, malformed-nesting, large-input, cleanup, and UI error regressions cover both synchronous and asynchronous import paths. | Implemented with shared conservative quotas and bounded readers; retain adversarial parser-budget regressions when import formats change. |
| SEC-005 (fixed) | Low | Update checks logged complete configured and response-provided URLs without redacting credentials or query tokens. | Both the configured update endpoint and response-provided download URL now pass through a fail-closed log formatter. Valid URLs emit only their normalized origin and either `/` or `/[redacted]`; userinfo, path content, query strings, and fragments are excluded. Malformed and opaque-origin URLs emit fixed placeholders. Regressions cover both URL sources, secret-bearing components, normalized hosts and ports, root and non-root paths, malformed URLs, and opaque origins. | Implemented with origin-only structured redaction and fixed failure placeholders; retain the log-capture regressions when update handling changes. |
| SEC-006 (fixed) | Low | Asset-effect capability tokens used a non-cryptographic UUID source and were not sender-bound. | Asset journals and renderer capabilities now use `crypto.randomUUID()`. Main keeps journal IDs private and grants renderer-document/root-bound capabilities that enforce the next valid direction, rotate after every successful effect, expire after 12 hours unless the live preload renews them, and throttle repeated failed lookups. Renderer destruction, process loss, and main-frame navigation revoke the session; in-flight and late mutations compensate before journal cleanup, root identity is rechecked against the journal, and committed mutations no longer become ambiguous when baseline refresh fails. Focused registry, IPC, host-history, journal-root, refresh-failure, renewal, teardown, race, and compensation regressions pass. | Implemented with cryptographic internal IDs, rotating one-shot renderer capabilities, document/root binding, expiry renewal, failed-lookup throttling, and teardown-safe compensation; retain these boundary regressions when asset history changes. |

| SEC-007 (fixed) | Medium | ZIP project import had no decompression quota and could exhaust temporary disk, CPU, or memory. | Native and TypeScript import paths now cap archives at 100 MiB, 10,000 entries, 100 MiB per expanded entry, 500 MiB cumulatively, 32 path components, and a 200:1 compression ratio; they reject unsafe paths and symlinks. The TypeScript fallback preflights every central-directory entry with direct `yauzl` access and rechecks during extraction. Native extraction enforces declared and actual streamed sizes. Both paths bound selected HTML and discovered assets, stream fallback directory discovery, revalidate copied asset size/containment, invalidate failed preparations, and remove temporary extraction data on the first quota or copy breach. Real-archive native tests and mocked TypeScript preflight/race/cleanup tests cover every quota dimension. | Implemented with preflight plus streaming enforcement and first-failure cleanup; retain real-archive coverage for the native path and add fallback integration coverage if it becomes a supported production backend. |

### Release and build findings

| ID              | Severity             | Finding                                                                                                                                | Evidence and impact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Recommendation                                                                                                                                                                                                                                                                 |
| --------------- | -------------------- | -------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| REL-001 (fixed) | Blocker              | The documented release command packages one host-native addon for incompatible OS/architecture targets.                                | `scripts/build-native.mjs:34-51,82-88` builds for the current host and writes one unqualified `twine_native.node`. `package.json:109,114` then asks electron-builder for Linux, macOS, and Windows in one run; `electron-builder.config.js:30-55` additionally requests macOS universal and Linux x64/arm64. The audit ran the configured macOS build: electron-builder successfully produced and checksum-verified a 225 MB universal DMG whose app executable contains x86_64 and ARM64 slices, but the packaged `twine_native.node` is ARM64-only. The x86_64 half therefore cannot load the required backend. Active CI only smokes host-native Linux/Windows x64 unpacked builds and has no macOS/ARM matrix (`.github/workflows/packaged-electron-smoke.yml:12-47`).                                                                                                                         | Build/package each target on a matching matrix runner (including macOS universal inputs and Linux ARM), keep native outputs target-qualified, assemble releases only after every target-specific smoke passes, and fail packaging on an ABI/target mismatch.                   |
| REL-002 (fixed) | High                 | Normal desktop and release builds can package stale checked-in WASM.                                                                   | `package.json:103,109,114,123,173` routes `build`, `dist`, and `start:electron` through paths that omit `build:wasm`; only the CI/performance `build:electron-app` variants run it (`:110-111`). The renderer imports generated files under `src/core/wasm/pkg`. A Rust/WASM source change can therefore pass a normal build and ship old generated code.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Make WASM generation or a deterministic generated-artifact freshness check part of every desktop/release build. Document and pin `wasm32-unknown-unknown` plus the matching `wasm-bindgen-cli`.                                                                                |
| REL-003 (fixed) | Medium               | The documented Rust lint gate fails on current stable Rust.                                                                            | The parser diagnostics and the warnings they had masked in later workspace crates are fixed. `.github/workflows/quality.yml` pins a Rust 1.88.0/1.96.0 matrix and runs formatting, all-target Clippy with `-D warnings`, and locked workspace tests on both. Both toolchains pass locally, including 252 workspace tests plus doc-tests per toolchain.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Implemented with a minimum/current pinned CI matrix; keep both versions aligned with the declared MSRV and reviewed current toolchain.                                                                                                                                         |
| REL-004 (fixed) | Low                  | The checked-in tree fails its JavaScript/Markdown formatting gate.                                                                     | `src/core/__tests__/story-index.test.ts` is formatted, `npm run format:check` passes, and the new quality workflow makes the formatting check required alongside tests, lint, documentation checks, and the production web build.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Implemented; keep `format:check` in the quality workflow.                                                                                                                                                                                                                      |
| REL-005 (fixed) | Medium               | The routine `npm run build` command is actually the full cross-platform packaging pipeline.                                            | `README.md:82-100` presents `npm run build` as an ordinary build/check and `npm run dist` as release creation. `package.json:103,109,114,123` defines both paths around the same clean-and-multi-platform electron-builder flow. This makes routine verification slow, platform-dependent, and vulnerable to REL-001.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Make `build` compile a host-runnable application without packaging; reserve explicit, target-specific scripts for release artifacts.                                                                                                                                           |
| REL-006 (fixed) | High                 | CI omits most of the stated quality contract and never tests installable artifacts.                                                    | `.github/workflows/quality.yml` runs unit/packaging tests, lint/documentation checks, formatting, the web build, and the two-version Rust gate. The target packaging workflow builds and retains Linux x64/ARM64 AppImage and ZIP outputs, macOS Intel/ARM64 DMGs, and a Windows x64 NSIS installer, then exercises AppImage launch, ZIP extraction, DMG mount, Windows install/uninstall, and packaged runtime smoke. Every per-target transfer archive is named `desktop-local-target-*`, includes `LOCAL-TEST-ONLY.txt`, and carries a local-profile manifest that distribution assembly rejects. The post-smoke job retains the manifest-bound `desktop-local-test-bundle` with a second explicit non-distribution notice. Workflow contract tests and every constituent host-available gate pass locally; non-host execution remains CI evidence to collect.                                  | Implemented with separate quality and target-installable workflows, explicitly local per-target transfer artifacts, and a post-smoke local-test-bundle assembly job.                                                                                                           |
| REL-007 (fixed) | High                 | Release organization succeeds even when required platform artifacts are missing.                                                       | `scripts/organize-release.mjs` defines the seven required OS/architecture/format slots and five target manifests. It validates exact names, profile/version/commit agreement, signing state, byte size, and SHA-256 before creating a temporary output; missing, duplicate, unexpected, dirty distribution, updater, or mismatched inputs fail without changing existing output. Validated files are copied into a whole-directory replacement after validation.                                                                                                                                                                                                                                                                                                                                                                                                                                   | Implemented with a seven-artifact, five-manifest matrix and pre-mutation validation; update the matrix, manifest schema, and tests together when supported targets change.                                                                                                     |
| REL-008 (fixed) | High                 | Artifact trust and distribution intent were ambiguous, allowing local unsigned or ad-hoc output to resemble publishable release input. | Packaging now has `local`, `distributable-unsigned`, and `signed` profiles with separate output roots. Local is the default and is rejected by distribution assembly; every retained CI artifact is explicitly named and marked as local-only, culminating in a labeled local test bundle. Deliberately unsigned distribution requires explicit acknowledgement, visible Windows/macOS filename labels, warnings, clean-source manifests, checksums, and absence of updater metadata. Signed packaging fails closed on missing or unexpected Windows/macOS identities and re-verifies Authenticode or Developer ID signing, notarization, and stapling from completed packages; Linux records signing as `not-applicable`. Target manifests bind profile, version, commit, date, platform/architecture, size, hash, and observed trust state to each artifact before aggregate assembly copies it. | Implemented. The release-process ambiguity is resolved. Absence of trusted publisher authentication remains an accepted residual risk for `distributable-unsigned`; checksums do not eliminate that risk, and the warning/provenance bundle must accompany every distribution. |
| REL-009 (fixed) | High                 | User-facing source/provenance metadata links to a different repository.                                                                | Package metadata and the About dialog's primary source action now point to `twine-rs-labs/twine.rs`. The About dialog retains `klembot/twinejs` as a separately labeled upstream repository, and the packaged ASAR contains the corrected repository URL. Component coverage asserts both destinations.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Implemented; keep the application source and separately labeled upstream attribution destinations distinct.                                                                                                                                                                    |
| REL-010 (fixed) | Medium               | The packaged app omits the project license and has incomplete third-party notice coverage.                                             | Every Electron target now packages root `LICENSE`, `THIRD_PARTY_NOTICES.md`, CycloneDX 1.6 `sbom.cdx.json`, and Electron's full `LICENSES.chromium.html`. The deterministic 249-component inventory covers the 83 npm identities actually shipped by Electron Builder, 135 reachable non-development Cargo packages, Electron/Chromium/Node/V8 at exact runtime versions, all three bundled font binaries by SHA-256, and all 24 story formats. The six formerly missing story-format licenses and ten upstream archives that declared MIT without a standalone notice now have reviewed full notices. The package hook extracts the final ASAR, verifies all four artifacts byte-for-byte, checks the complete npm identity multiset and font hashes against the SBOM, and rejects structural SBOM or inventory drift. A real unpacked macOS ARM app passed that gate.                            | Implemented; regenerate compliance artifacts on every packageable build and retain the final package-content gate when dependencies, runtimes, fonts, or story formats change.                                                                                                 |
| REL-011         | Medium               | Project release notes, tags, and a completed release checklist are absent.                                                             | Repository controls now include `CHANGELOG.md`, `RELEASING.md`, `SUPPORT.md`, a release checklist issue form, approved JSON release plans, strict version/tag/changelog/release-manager-approval/rollback checks, tag-triggered draft assembly, manually dispatched checklist-gated publication, generated release records, durable GitHub Release assets, and fresh-download smoke jobs. The policy explicitly supports a solo maintainer without a second reviewer or publication environment. Repository settings must still protect `v*` and enable release immutability before use. Historical tags were deliberately not invented: `git tag --list` remains empty, no release has exercised the workflow, and no checklist has completed Post-publication closeout. The policy and enforcement portion is implemented, but closure evidence does not yet exist.                              | Apply the governed process to the next intentional prerelease or stable release; close this finding only after its protected annotated tag, curated notes, immutable artifacts, retained provenance, and completed checklist can be cited.                                     |
| REL-012 (fixed) | Medium               | Builds are not reproducible or toolchain-pinned.                                                                                       | `rust-toolchain.toml` pins Rust 1.96.0, `rustfmt`, Clippy, and the WASM target; the WASM script invokes the rustup proxy instead of a mutable direct toolchain path. Active workflows pin Node 24.18.0, fixed runner images, and every external action to an immutable Node 24-runtime commit. Native/WASM Cargo builds remain locked. Web archives now use one Node ZIP implementation with sorted paths, fixed timestamps, normalized permissions, and atomic writes; Workbox's glob-derived precache entries are sorted, manifest icons are injected only once, and the generated URL list is checked for canonical order and uniqueness. The quality gate performs two clean production web builds and requires identical SHA-256 output. The checked-in WASM regenerated twice to the same hash under the pinned toolchain.                                                                   | Implemented; update toolchain versions, immutable action revisions, archive tests, and the two-build hash gate together.                                                                                                                                                       |
| REL-013 (fixed) | Medium (conditional) | Rust crate publication intent is undefined and the manifests are not publication-ready.                                                | The workspace now declares `publish = false`, and all ten internal crates explicitly inherit that policy. A metadata-level regression test requires the exact workspace crate set and verifies that Cargo resolves every member to an empty publication registry list, preventing accidental crates.io publication.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Implemented; retain the inherited non-publication policy and update the exact-set metadata test deliberately if workspace membership changes.                                                                                                                                  |
| REL-014 (fixed) | Low                  | Release and preview raster icons are byte-identical.                                                                                   | The preview and release PNG/ICO assets, including the no-padding variants, are restored from the last known-good versions that match the distinct source artwork. All four preview/release pairs now differ byte-for-byte, and `scripts/__tests__/app-icons.test.mjs` guards the distinction in the packaging test suite.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Implemented; retain the pairwise asset assertion when regenerating or replacing application artwork.                                                                                                                                                                           |
| REL-015 (fixed) | Low                  | Prerelease detection and version bumping disagree.                                                                                     | Explicit valid SemVer prerelease versions such as `0.2.0-beta.1`, including valid build metadata, are accepted and propagated across npm and Cargo metadata. Invalid numeric prerelease identifiers remain rejected. Electron preview detection now treats any SemVer prerelease as a preview rather than recognizing only selected words. Focused dry-run tests cover beta, release-candidate, preview, build-metadata, and invalid forms.                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Implemented with the minimal explicit-version support needed for beta distribution; keep unrelated version automation outside this finding.                                                                                                                                    |
| REL-016 (fixed) | Medium               | The packaged macOS app declares broad sensitive-device access with generic purpose text.                                               | The default session now permits only sanitized clipboard writes from the trusted main frame. Permission checks and requests from embedded story content are denied, as are display capture, HID, serial, USB, Bluetooth selection/pairing, and all other Electron permissions. macOS packaging explicitly removes the five inherited Electron usage-description keys; direct inspection of a packaged ARM app confirms they are absent. Unit and packaging tests cover the policy and plist configuration.                                                                                                                                                                                                                                                                                                                                                                                         | Implemented; add an explicit narrowly scoped policy and precise purpose text before introducing any future device capability.                                                                                                                                                  |

### Remaining release/dependency fix order

This order reflects shipped/release risk, unblock value, and implementation dependencies as of 2026-07-29. Both remaining prioritized findings are P1 work before a release candidate; fixed findings have been removed.

| Rank | Priority | Finding | Required treatment                                                                                                                                                            |
| ---: | -------- | ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|    1 | P1       | DEP-001 | Refresh `fast-uri` to at least 3.1.4 within the existing transitive range, verify build/package behavior, and add explicit full-versus-production npm audit policy to CI.     |
|    2 | P1       | REL-011 | Exercise the implemented release policy and workflow for the first intentional release, then retain its protected tag, immutable assets, provenance, and completed checklist. |

### Documentation findings

| ID              | Severity | Finding                                                                                      | Evidence and impact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Recommendation                                                                                                                                                                           |
| --------------- | -------- | -------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DOC-002 (fixed) | High     | The served installation manual directs twine.rs users to install the upstream Twine product. | The served installation and update entries are now explicit Twine RS compatibility notices. They link to the canonical current-availability page under `docs/user/`, which states that no formal downloadable Twine RS release exists yet, directs source users to the root README, and distinguishes upstream Twine as a separate product. The mdBook 0.5.4 output preserves both links as absolute repository-rendered documentation URLs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Implemented; keep availability and source-setup claims synchronized with shipped distribution behavior.                                                                                  |
| DOC-003 (fixed) | Medium   | The updater manual claims a functional check that normally performs no network check.        | The canonical and served update documentation now state that current builds have no operated update metadata feed and automatic updates are unavailable. When `TWINE_RS_UPDATE_URL` is absent, `check-for-update.ts` performs no fetch and shows the localized unavailable/not-configured informational message; configured same-version behavior still reports up to date. Focused tests cover both paths.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Implemented; do not report up to date unless a configured update check actually completes.                                                                                               |
| DOC-004 (fixed) | Medium   | Contributor setup omits required browser-test installation.                                  | `README.md` now makes Playwright provisioning conditional on browser end-to-end work: `npx playwright install` installs all configured Chromium, Firefox, and WebKit binaries, while Linux contributors use `npx playwright install --with-deps` for browsers plus system packages. Unit, lint, and documentation-only work is explicitly exempt. Browser binaries were not installed and the browser suite was not rerun in this documentation batch; the audit's existing E2E evidence remains separate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Implemented; keep the conditional bootstrap commands aligned with the configured Playwright projects and supported Linux environment.                                                    |
| DOC-005 (fixed) | Medium   | Documentation validation excludes the served manual.                                         | `npm run check:docs` now covers `docs/en/src` and passes across 126 Markdown and 29 authoritative design-system HTML files. The compatibility manual has a structured upstream-compatibility marker, canonical repository links, semantic title/scope enforcement, and a book-source boundary that rejects escaping local links. Four real dead inherited release-note targets were minimally repaired. Quality CI installs exact `mdbook` 0.5.4 with `--locked` and runs the real `npm run build:docs`; generated output preserves the canonical HTTPS documentation links without escaping relative HTML targets.                                                                                                                                                                                                                                                                                                                                                                                                                                            | Implemented; retain served-manual link/ownership checks, canonical link boundaries, the pinned builder, and the real CI build.                                                           |
| DOC-006 (fixed) | High     | In-product Help sends users directly to upstream Twine documentation.                        | The workbench toolbar and native desktop Help menu now share one URL constant targeting the current repository-rendered Twine RS user-documentation landing page. No runtime Help path targets the upstream Twine guide, and focused tests cover both native menu variants and toolbar registration. Immutable release documentation references remain future release evidence because no formal release or tag exists yet.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Implemented; select an immutable documentation reference as part of a future release when release evidence exists.                                                                       |
| DOC-007 (fixed) | High     | Troubleshooting instructions target upstream Twine data and backup locations.                | `docs/user/recovery-and-backups.md` is now the canonical shipped-behavior guide for the derived `<appData>/twine-rs` settings directory, reversible recovery of both `prefs.json` and `app-prefs.json`, localized `Documents/Twine RS/{Stories,Backups}` defaults, scheduled backup cadence/retention, project-data boundaries, and destructive folder-overlap risks. Both served troubleshooting pages are short Twine RS compatibility notices with absolute repository-rendered links to the canonical guide. Live target-native default-path confirmation remains platform evidence rather than a closure blocker.                                                                                                                                                                                                                                                                                                                                                                                                                                         | Implemented; keep recovery and backup guidance synchronized with desktop path derivation, retention behavior, and platform evidence.                                                     |
| DOC-008 (fixed) | High     | Served graph-navigation controls contradict the current workbench.                           | `docs/user/graph-navigation.md` now derives the current input map from the normative workbench contract, including cursor-anchored wheel zoom, Shift-wheel pan, Space/middle/Pan-tool drag, marquee and node interaction, context actions, tool/zoom/Fit keys, and editor-safe deletion. The served navigation page links to it with an absolute repository-rendered URL. Focused component coverage includes the added Shift-wheel, Pan-tool primary-drag, right-drag rejection, and keyboard tool/zoom/Fit contracts alongside the existing wheel, middle/Space drag, selection, move, open, context, and deletion tests.                                                                                                                                                                                                                                                                                                                                                                                                                                    | Implemented; keep the canonical input map and focused UI-contract coverage synchronized with the workbench.                                                                              |
| DOC-009 (fixed) | Medium   | The advertised Rust CLI has no conventional `--help`.                                        | The developer-facing `twine_cli` engineering binary is now explicitly distinguished from the packaged desktop executable. The real `twine-rs` binary supports stable `--help`/`-h` and `--version` contracts before file access, lists every implemented command and compatibility form, rejects missing arguments, malformed known-command arity, and unknown switches with usage, and retains one positional input path as backward-compatible inspect. Six dependency-free black-box integration tests execute the real binary through `CARGO_BIN_EXE_twine-rs`. Cross-platform execution remains routine platform evidence rather than a closure blocker.                                                                                                                                                                                                                                                                                                                                                                                                  | Implemented; keep the developer CLI's help, version, dispatch behavior, integration tests, and developer-versus-packaged distinction synchronized.                                       |
| DOC-010 (fixed) | Medium   | Desktop command-line examples use obsolete executables and omit supported switches.          | `command-line.ts` now has one structured schema that generates parser aliases/types, app-pref override membership, and complete help text with bounds and deprecation status. `docs/user/desktop-command-line.md` embeds that exact generated help surface, uses package-correct macOS, PowerShell, and Linux examples, documents positional `.twine.rs` folders and destructive folder risks, and is linked from the served compatibility page by an absolute repository-rendered URL. `app-prefs.ts` keeps CLI overrides runtime-only and persists only file or explicit settings. Accepted ADR 0005 has a dated implementation clarification, with parity coverage, that distinguishes the generated usage name and Linux executable `twine-rs`, macOS `Twine RS` inside `Twine RS.app`, and Windows `Twine RS.exe`; portable `--help`/`-h` behavior is expressed against the platform executable rather than implying one universal filename. Live target-native packaged executable confirmation remains platform evidence rather than a closure blocker. | Implemented; derive parser/help membership from the schema and keep the exact guide, package-specific examples, and ADR parity assertions under test.                                    |
| DOC-011 (fixed) | Medium   | The root “working” workbench UI-kit example references missing assets.                       | All seven stale root files under `ui_kits/workbench/` were deleted. The six-file `docs/design-system/ui_kits/workbench/` kit is now the sole authoritative copy, and every local CSS, script, SVG, and JSX reference in the authoritative HTML set resolves. Active `ui_kits_remediation/LIGHT_THEME_IMPLEMENTATION.md` guidance now references only `docs/design-system/ui_kits/workbench/...`. The checker rejects both nonempty legacy root content and stale root `ui_kits/workbench` paths in active remediation guidance while accepting canonical paths.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Implemented; keep the canonical six-file kit self-contained and retain resource, legacy-content, and active-remediation path validation.                                                 |
| DOC-012 (fixed) | Low      | Architecture/design/status pages retain several stale contracts.                             | The Rust workspace and style guide now describe the current implemented crates and enduring application-integration contracts rather than M0/future milestones, with current ownership and verification metadata; `crates/README.md` enumerates all ten active workspace crates, including `twine_native` and `twine_wasm`. The design implementation guide identifies the real `src/components/design-system/index.ts` barrel and a truthful relative import; documentation validation rejects both nonexistent `@twine/ui` guidance and loss of the real import contract. Performance status now identifies the tracked July 16 10k matched reference and July 21 50k clean target-only reference, preserves the earlier 50k record as historical evidence, and does not fabricate a rerun. ADR 0005's accepted AppImage+ZIP and manual-update decision remains unchanged, while a dated executable-name clarification corrects the old bare `twine` spelling and preserves the platform-specific names.                                                     | Implemented; keep the complete workspace inventory, current-state metadata, checked design imports, tracked-reference provenance, and accepted ADR contracts synchronized with the tree. |

## Verification matrix

| Check                                               | Result            | Notes                                                                                                                                                                                                                                                                    |
| --------------------------------------------------- | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Repository/worktree inventory                       | Pass              | Baseline revision recorded; pre-existing untracked `.codex/` preserved.                                                                                                                                                                                                  |
| JavaScript/TypeScript lint and documentation checks | Pass              | `npm run lint`; documentation validation covers 126 Markdown and 29 authoritative design-system HTML files.                                                                                                                                                              |
| JavaScript/TypeScript unit and packaging tests      | Pass              | 277 Jest suites; 2,247 pass, 3 skip, 19 todo. All 36 packaging tests pass.                                                                                                                                                                                               |
| Browser end-to-end tests                            | Pass              | All 18 Playwright scenarios pass across Chromium, Firefox, and WebKit.                                                                                                                                                                                                   |
| Targeted web persistence reproductions              | Pass              | Hydration and colliding-ID regressions pass in Chromium, Firefox, and WebKit; quota failures are injected at every transaction write boundary.                                                                                                                           |
| Web production build                                | Pass with warning | Two clean `npm run build:web` executions produce an identical normalized web archive; the main JS chunk remains 1.59 MB and triggers Vite's chunk-size warning.                                                                                                          |
| Full host Electron application build                | Pass with warning | Pinned-toolchain WASM, web, native ARM addon, and Electron main build succeed; the web chunk warning remains.                                                                                                                                                            |
| Host unpacked package and packaged-app smoke        | Pass with caveats | macOS ARM app built; staged and final-ASAR addon checks pass, the corrected provenance metadata and permission-policy module are packaged, and direct plist inspection confirms all five unsupported device declarations are absent. Signing is ad hoc; no notarization. |
| Target-specific desktop packaging contract          | Pass on host      | `npm run dist` produces an ARM64 DMG; an intentional x64 request fails before packaging. Non-host matrix jobs remain for CI.                                                                                                                                             |
| JavaScript/Markdown formatting                      | Pass              | `npm run format:check`.                                                                                                                                                                                                                                                  |
| Rust formatting                                     | Pass              | `cargo fmt-check`.                                                                                                                                                                                                                                                       |
| Rust Clippy                                         | Pass              | All-target workspace Clippy passes with `-D warnings` on Rust 1.88.0 and 1.96.0.                                                                                                                                                                                         |
| Rust workspace tests                                | Pass              | 252 unit tests and all doc-tests pass on Rust 1.88.0 and 1.96.0.                                                                                                                                                                                                         |
| npm dependency audit                                | Fail (dev only)   | DEP-001 currently comprises one high-severity `fast-uri@3.1.3` development-tool advisory group; the production graph is unaffected.                                                                                                                                      |
| Cargo/RustSec dependency audit                      | Pass              | `crossbeam-epoch@0.9.20` and `anyhow@1.0.104`; `cargo-audit 0.22.2 --deny warnings` reports no findings against database commit `b5fc89b8`. A dedicated CI workflow checks Cargo dependency changes and runs weekly.                                                     |
| English mdBook build                                | Pass              | `mdbook 0.5.4 build docs/en` succeeds.                                                                                                                                                                                                                                   |
| Correctness and data-integrity review               | Pass              | All twenty-three correctness findings are fixed.                                                                                                                                                                                                                         |
| Electron security boundary review                   | Pass              | SEC-001 through SEC-007 are fixed; the release follow-up also installs a complete deny-by-default Electron session and device-permission policy.                                                                                                                         |
| Electron packaging/release configuration review     | Fail              | REL-011 remains open; REL-008 through REL-010 and REL-012 through REL-016 are fixed.                                                                                                                                                                                     |
| Documentation cross-check                           | Pass              | DOC-001 through DOC-012 are fixed; no documentation finding remains open.                                                                                                                                                                                                |

## Audit log

- 18:57 CEST — Began audit at `94eaa0031`; confirmed local Node 24.18.0, npm 11.18.0, Rust 1.96.0, and Cargo 1.96.0.
- 18:57 CEST — Confirmed DOC-001 by comparing `README.md`, `.nvmrc`, and `package.json`.
- 18:58 CEST — Installed the lockfile with `npm ci`; npm reported two high-severity development-tool advisories.
- 19:00 CEST — `npm run build:web` passed with a chunk-size warning.
- 19:02 CEST — JavaScript unit and packaging tests passed; formatting failed on one tracked test.
- 19:04 CEST — Rust formatting and all workspace tests passed; Rust Clippy failed on three warnings promoted to errors.
- 19:06 CEST — Confirmed the cross-target native packaging blocker, stale-WASM build path, and Electron story-format trust-boundary issue.
- 19:12 CEST — RustSec audit found one vulnerable and one unsound locked Rust dependency; reachability review found no direct use of the triggering APIs.
- 19:14 CEST — Completed security, documentation, and release-configuration reviews; ordinary workflow correctness review continues.
- 19:23 CEST — Paused at user request with all findings saved incrementally.
- 22:44 CEST — Resumed at user request, counting only active audit time against the one-hour limit.
- 22:48 CEST — Built and verified the configured universal macOS DMG; confirmed its application executable is universal but its required native addon is ARM64-only.
- 23:06 CEST — Residual tracked-file credential-pattern scan found no private key or common provider-token signature; the tracked `.env` contains package metadata substitution only.
- 23:09 CEST — Artifact metadata review confirmed consistent version/identifier but found unnecessary generic macOS sensitive-device usage descriptions (REL-016).
- 23:11 CEST — Residual web-persistence review identified deterministic passage-body and cross-story record loss plus non-atomic quota failure handling (BUG-021 through BUG-023).
- 23:11 CEST — Residual security review found unbounded direct imports and two lower-severity hardening gaps (SEC-004 through SEC-006); no committed secrets were found.
- 23:19 CEST — Added ZIP decompression-quota finding (SEC-007), reconciled 60 findings, and closed the audit at the one-hour active-time limit.
- 23:20 CEST — User authorized a short overrun to account for interruptions; reopened only for final consistency and formatting checks.
- 23:21 CEST — Prettier, documentation validation, ID continuity, duplicate detection, severity totals, and worktree-scope checks passed; audit completed.
- 23:40 CEST — User-authorized extension reproduced BUG-021 and BUG-022 in isolated Chromium profiles, corrected BUG-021's restart behavior, and strengthened both findings with runtime evidence.
- 2026-07-22 03:34 CEST — Fixed BUG-007 by merging full saves by stable story ID, preserving sibling sources and renderer metadata in the atomic store transaction, enabling backups, making the legacy fallback fail closed for multi-story projects, and adding native, Electron, and packaged-app save/rename regressions.
- 2026-07-22 11:26 CEST — Marked BUG-009 and BUG-021 fixed after `bcc6e45b3` made post-hydration duplicate/import flows document-aware and preserved hydrated passage bodies across selection and reload.
- 2026-07-22 11:52 CEST — Marked BUG-017 and BUG-020 fixed after `22ec18c1` preserved unmanaged project entries and added guarded native-root replacement with rollback and recovery tests.
- 2026-07-22 12:26 CEST — Fixed BUG-022 and BUG-023 with story-scoped immutable passage records, a revisioned single-manifest commit point, failed-write cleanup, legacy migration, linear batch indexing, and mixed-document recovery. Focused hydration and collision scenarios passed in all three browser engines; quota injection covered every record/manifest write boundary; lint, the web build, focused tests, and independent external review passed.
- 2026-07-22 22:12 CEST — Fixed SEC-006 with cryptographic journal IDs, rotating renderer-document/root-bound capabilities, expiry renewal, failed-lookup throttling, and teardown-safe compensation for in-flight and late asset mutations. Focused and full unit tests, Electron and web builds, lint, packaging tests, documentation checks, and independent external review passed.
- 2026-07-22 13:31 CEST — Fixed BUG-008 with story-scoped passage-layout keys, schema-2 migration and reader gating, deletion/replacement cleanup, story-scoped Electron saves and watcher deltas, metadata-preserving coordinate updates, and authoritative external-delta undo/redo. Duplicate-ID, delete/recreate, empty-layout, direct-save, native-merge, watcher, and incremental-save regressions pass; 241 Rust tests, 2,114 JavaScript/TypeScript tests, 16 packaging tests, TypeScript/ESLint checks, and the full host Electron build pass. Independent external review completed with no remaining actionable findings.
- 2026-07-22 15:46 CEST — Fixed BUG-001 and BUG-002 with acknowledged asynchronous IPC writes, renderer-visible failure handling, per-story save/delete/rename ordering, and a single-flight `before-quit` barrier spanning debounced main-process writes and synchronously reserved renderer work. Last-window close now keeps the renderer alive through the barrier; renderer destruction, process loss, and cross-document navigation reject orphaned reservations. Direct story launch failures and scratch-file launch failures have terminal handling. Verification passed with 273 Jest suites and 2,139 tests, 16 packaging tests, lint, both TypeScript configurations, the full host Electron build, an unpacked macOS ARM package, and a packaged regression that closes the actual `BrowserWindow` before a delayed trailing save begins. Independent external review completed with no remaining actionable findings.
- 2026-07-22 16:21 CEST — Fixed REL-001, REL-002, and the overlapping REL-005 by replacing cross-platform packaging with target-specific host builds, storing native addons under platform/architecture-qualified paths, inspecting Mach-O/ELF/PE headers before packaging and inside the final ASAR, and expanding packaged smoke coverage to Linux x64/ARM64, macOS Intel/ARM64, and Windows x64 runners. Every desktop/release path now regenerates WASM with the Cargo-locked `wasm-bindgen` CLI version. The exact host `npm run dist` flow produced an ARM64 DMG; an intentional x64 package request failed before download/packaging. All 273 Jest suites and 2,139 tests, all 24 packaging tests, lint, documentation checks, the host build, and two focused packaged-app regressions pass. The pre-existing formatting failure remains isolated to REL-004. Independent external review found and prompted fixes for Windows ASAR separators and README/audit accuracy, then completed with no remaining actionable findings.
- 2026-07-22 16:55 CEST — Fixed SEC-002 and SEC-003. Scratch previews now use main-allocated exclusive directories and filenames, accept only logical indexed asset requests, read support files as bounded owned bytes through the native anchored no-follow project capability, fail closed on baseline/read failures, cap HTML and asset payloads at 50 MiB each, serialize creation, and retain at most three previews. All OS URL handoffs now share a credential-free HTTPS-only validator, including update responses, navigation, popups, and Help. Verification passed with 274 Jest suites and 2,152 tests, 24 packaging tests, 243 Rust tests plus doc-tests, lint/documentation checks, both TypeScript configurations, the web production build, the native addon build and ABI probe, and the Electron main build. The pre-existing formatting and Clippy findings remain open as REL-004 and REL-003. Independent external review identified and prompted fixes for no-follow asset reads and aggregate scratch retention, then completed with no remaining actionable findings.
- 2026-07-22 17:08 CEST — Fixed BUG-016 by capturing each deletion's storage kind before the React reducer dispatch, preventing native project deletions from invoking the legacy HTML bridge in either batch or direct persistence paths, retaining project metadata through command application, and enumerating every filesystem and library target in confirmations. Coexisting and missing legacy-file regressions, multi-story target enumeration, storage-kind capture, and metadata ordering are covered. All 274 Jest suites and 2,157 tests, targeted ESLint, the production web build, Prettier, and diff checks pass. Independent external review completed with no actionable findings; full real-filesystem Electron coexistence remains an integration-test gap, while the host, route, and persistence decision boundaries are covered.
- 2026-07-22 17:23 CEST — Fixed DEP-002 by locking `crossbeam-epoch@0.9.20` and `anyhow@1.0.104` without manifest or feature changes and adding a path-filtered plus weekly RustSec CI gate pinned to `cargo-audit 0.22.2` with `--deny warnings`. All-target workspace checking, Rust formatting, 243 workspace tests plus doc-tests, exact dependency-tree checks, and a strict audit of 163 locked dependencies against RustSec database commit `b5fc89b8` pass. Clippy remains limited to the three pre-existing REL-003 diagnostics. Independent external review identified and prompted the strict warning-policy correction in CI, then completed with no remaining actionable findings.
- 2026-07-22 18:50 CEST — Fixed SEC-004 and SEC-007 with pre-read and opened-handle source limits, bounded HTML/Twee parsing and metadata work, ZIP central-directory preflight plus streaming quota enforcement, bounded streaming asset discovery/copying, symlink and containment checks, and first-breach temporary-data cleanup. Limits cover archive/source/entry/aggregate bytes, entry/story/passage/tag/asset counts, compression ratio, and nesting depth; 10 MiB adversarial parser and asset-rewrite regressions exercise the reviewed CPU paths. Verification passed with 275 Jest suites (2,192 pass, 3 skip, 20 todo), 24 packaging tests, 250 Rust workspace tests plus doc-tests, lint/documentation checks, both TypeScript configurations, the web production build, the Electron main build, Rust formatting, and diff checks. The existing REL-003 Clippy and REL-004 formatting findings remain open. Independent external review prompted fixes for HTML tokenizer recovery, nested-story amplification, parser and rewrite complexity, bounded asset scanning, symlink/copy races, quota cleanup, and retry state, then completed with no remaining actionable findings. TypeScript ZIP tests mock `yauzl`/`extract-zip` while native tests exercise real archives; Node path containment is revalidated rather than directory-FD-anchored against a same-user namespace race.
- 2026-07-22 20:02 CEST — Fixed BUG-012 and BUG-013 by returning search and replace ranges in JavaScript UTF-16 units, preserving original source spans through per-code-point fuzzy normalization, making long Unicode excerpts boundary-safe, and aligning Rust character and line metrics with JavaScript and CodeMirror across analysis files, Contents entries, passage facts, cached story summaries, and incremental edits. Regressions cover emoji, accented text, expanding lowercase, context-sensitive Greek sigma, long Unicode lines, non-ASCII metrics, terminal newlines, and source-editor navigation. All 145 `twine_core` tests plus doc-tests, 46 focused Jest tests, targeted ESLint, Rust formatting, focused Prettier checks, documentation checks, and diff checks pass; the pre-existing REL-004 file-level formatting failure remains unchanged. Independent external review prompted the fuzzy-normalization and excerpt-boundary hardening, then completed with no remaining actionable findings.
- 2026-07-22 20:27 CEST — Fixed REL-003, REL-004, REL-006, and REL-007. The checked-in tree passes formatting and all-target Clippy; dedicated quality CI now runs the JavaScript, documentation, web-build, and pinned Rust 1.88.0/1.96.0 contracts. Target-native jobs build, retain, install or mount, and smoke AppImage, ZIP, DMG, and NSIS outputs, while final assembly requires exactly seven canonical artifacts and fails before mutation on missing, duplicate, stale, or unexpected inputs. Verification passed with 275 Jest suites (2,206 pass, 3 skip, 20 todo), 28 packaging tests, lint/documentation checks, the production web build, formatting, and 252 Rust workspace tests plus doc-tests and Clippy on both pinned toolchains. Independent external review identified and prompted rejection of collision-suffixed duplicate artifacts plus README/audit workflow corrections, then completed with no remaining actionable findings. Non-host installer execution remains pending the matrix's first GitHub Actions run.
- 2026-07-22 20:42 CEST — Fixed BUG-003, BUG-005, and BUG-014 by making persisted story and passage defaults locale-independent and safe before i18n bootstrap, localizing explicit untitled-passage creation at its action boundary, correcting empty-IFID repair without replacing a valid IFID during ID repair, and invoking passage defaults in both partial materialization paths. The nested creation contract now accepts ID-bearing partial passage documents and preserves supplied text. Missing-name legacy migration, inverse IFID, partial load/create, and distinct localized-name regressions pass; the full Jest suite, lint/documentation checks, TypeScript, the production web build, formatting, and diff checks pass. Independent external review prompted the localized-name regression and document-aware partial type, then completed with no remaining actionable findings.
- 2026-07-22 21:08 CEST — Fixed BUG-018 by placing every scratch preview and its copied assets in an opaque per-process and per-preview namespace, preserving recent previews owned by concurrent app processes, removing expired foreign-session roots, and replacing silent live-preview eviction with a 300 MiB aggregate session quota. The full write-and-open lifecycle is serialized; quit synchronously rejects new launches, waits for in-flight launches and pending story writes, cleans only after a successful flush, and resumes launches without deleting previews if quit is canceled. Colliding, missing, foreign-session, more-than-three-live, quota, partial-write, failed-launch, in-flight-quit, and failed-quit regressions pass. Verification passed with 275 Jest suites (2,218 pass, 3 skip, 19 todo), lint/documentation checks, formatting, the Electron main TypeScript build, and diff checks. Independent external review identified and prompted fixes for live-root eviction, launch/cleanup races, cross-process ownership, and canceled-quit cleanup, then completed with no remaining actionable findings.
- 2026-07-22 21:20 CEST — Fixed BUG-004 by making “Start Empty Here” accept only a readable folder with no entries before changing the preference, in-memory path, or relaunch state; nonempty and unreadable folders leave the current library intact with an explanatory error. All four dialog responses are covered, including empty, nonempty, and read-error regressions for the distinct start-empty path. Verification passed with 275 Jest suites (2,221 pass, 3 skip, 19 todo), lint/documentation checks, the Electron main TypeScript build, formatting, and diff checks. Independent external review completed with no actionable findings.
- 2026-07-22 21:39 CEST — Fixed BUG-019 by retaining the startup queue while adding path-free main-to-renderer notifications, subscribing before the initial renderer drain, acknowledging requests through serialized queue consumption, coalescing notifications received during an in-flight drain, and cleaning up both notification endpoints. Startup, post-startup, in-flight, StrictMode, unmount, and window-close regressions pass; the full Jest suite (275 suites: 2,226 pass, 3 skip, 19 todo), targeted ESLint, both production TypeScript builds, repository formatting, documentation validation, and diff checks pass. Independent external review completed with no actionable findings after prompting the in-flight and cleanup regressions. A single test does not drive the actual `app.on('open-file')` registration through the preload bridge, but the queue notification, main-window forwarding, and renderer subscription and consumption boundaries are covered separately.
- 2026-07-22 22:19 CEST — Fixed SEC-005 by logging only normalized URL origins plus fixed path markers for configured and response-provided update URLs; userinfo, path content, queries, and fragments are excluded, while malformed and opaque-origin inputs produce constant placeholders. Root and non-root paths, non-default ports, both secret-bearing URL sources, and failure placeholders are covered. Verification passed with 276 Jest suites (2,244 pass, 3 skip, 19 todo), lint and documentation checks, repository formatting, the Electron main-process build, and diff checks. Independent external review completed with no actionable findings after its non-blocking root-path and non-default-port coverage note was incorporated.
- 2026-07-22 23:14 CEST — Fixed DOC-001, REL-009, REL-012, and REL-016. Source metadata now identifies Twine RS while retaining separately labeled upstream attribution. Builds use pinned Rust 1.96.0 through the rustup proxy, Node 24.18.0, current Node 24-runtime GitHub Actions at immutable revisions, deterministic ZIP metadata, and a repeat-build gate that also validates one canonical, duplicate-free PWA precache. Electron applies a main-frame, renderer-document-bound clipboard permission allowlist, denies all other permission and device requests, and packages macOS without unsupported sensitive-device usage declarations. Verification passed with 277 Jest suites (2,247 pass, 3 skip, 19 todo), 36 packaging tests, lint/documentation/format checks, Rust 1.88.0 and 1.96.0 formatting, Clippy, 252 workspace tests plus doc-tests, the full host build, packaged ARM plist/ASAR/signature inspection, a twice-identical WASM hash of `3a990b38b117005f5f0d5ab18f4a2625b50d8d358605d984cb1d708025741fff`, and a twice-identical web archive hash of `157a497a606b9b9708b5429ba8b5b704773efd83a1723b4e3ed1a23b4f5eefdc`. The macOS app remains ad-hoc signed and unnotarized under REL-008. Independent external review identified and prompted fixes for a direct stable-toolchain bypass, EOL and stale Action pins, duplicate PWA precache icons, generated-manifest validation, and audit-count drift, then completed with no remaining actionable findings.
- 2026-07-23 00:01 CEST — Fixed REL-010 with deterministic production npm/Cargo/runtime/font/story-format inventories, complete embedded and standalone license notices, CycloneDX 1.6 output, and final-ASAR verification of root artifacts, the packaged npm identity multiset, and bundled font hashes. The real macOS ARM package passed the 249-component gate. Verification passed with 277 Jest suites (2,247 pass, 3 skip, 19 todo), 42 packaging tests, lint/documentation/format checks, deterministic repeat generation, and the unpacked host package. Independent external review found the initial runtime, font, metadata-only MIT notice, and lockfile-traversal gaps; after correction it verified the final ASAR and completed with no remaining findings.
- 2026-07-23 11:04 CEST — Rechecked and prioritized the remaining release/dependency findings after the transitive lock refresh. The earlier `brace-expansion` and `shell-quote` advisories are fixed; `npm audit` now reports only development-only `fast-uri@3.1.3` under Electron Builder and Workbox. REL-008 remains the first release task: implement the unsigned-versus-publishable artifact guard now, while actual macOS Developer ID signing and notarization remain externally deferred. The remaining order is DEP-001, REL-011, REL-013, REL-015, then REL-014.
- 2026-07-23 12:48 CEST — Fixed REL-008 and the beta-blocking portion of REL-015 with explicit local, deliberately unsigned, and trusted-signing release profiles; exact seven-artifact/five-manifest assembly; native package trust inspection; clean-source provenance; updater-metadata rejection; local-only CI transfer and aggregate notices; and valid SemVer prerelease handling. A real local macOS ARM directory package and DMG passed, with the final manifest recording ad-hoc app signing and no notarization or stapling. Verification passed with 277 Jest suites (2,247 pass, 3 skip, 19 todo), 66 packaging tests plus one Windows-only process test skipped on macOS, lint/documentation/format and diff checks, and independent external review with no remaining actionable findings. Credentialed Windows Authenticode and macOS Developer ID/notarization execution remains release-run evidence to collect.
- 2026-07-23 — Fixed REL-013 by declaring the Rust workspace internal and non-publishable, requiring all ten crates to inherit `publish = false`, and adding a Cargo metadata regression under both pinned Rust toolchains that checks the exact workspace crate set and every member's effective publication policy. Verification passed with all 252 Rust tests plus doc-tests, 67 packaging tests (66 pass and one Windows-only skip), Cargo 1.88.0 and 1.96.0 metadata checks, an expected `cargo publish --dry-run` refusal, documentation and formatting checks, and diff hygiene. Independent external review identified and prompted moving the metadata gate from the JavaScript job into the pinned Rust matrix, then completed with no remaining findings.
- 2026-07-29 — Implemented the repository-control portion of REL-011 with a canonical Twine RS changelog, release and support policies, a durable checklist form, approved release-plan and generated-record schemas, strict candidate/publication/closeout validation, target-native tag-triggered draft assembly, manually dispatched solo-maintainer publication without a protected environment, immutable evidence verification, and release-hosted Windows/macOS/Linux smoke checks. The finding remains open by design until repository settings are configured and the first intentional release produces the protected tag and completed checklist that the audit requires; no historical release evidence was fabricated.
- 2026-07-29 — Fixed REL-014 by restoring all eight release and preview PNG/ICO assets from their last known-good versions, making the channel-specific artwork visibly and bytewise distinct again. A packaging regression now compares all regular and no-padding preview/release pairs. Verification passed with 85 packaging tests and one platform-specific skip, focused icon testing, formatting, and diff checks.
- 2026-07-29 — Fixed DOC-002, DOC-003, and DOC-006 with canonical current-availability and update documentation, safe served compatibility notices, an explicit unavailable update-check result when no feed is configured, and shared current repository-rendered Twine RS Help documentation. Verification passed with three focused Jest suites and 50 tests, both TypeScript configurations, full lint, documentation, and formatting checks, an mdBook 0.5.4 build with generated absolute-link inspection, and diff checks. Independent repeat documentation review and QA completed with no actionable findings. Immutable Help references remain future release evidence because no formal release or tag exists yet.
- 2026-07-29 — Fixed DOC-007, DOC-008, and DOC-010 with canonical shipped-behavior recovery/backups, graph-navigation, and desktop command-line guides plus safe served compatibility notices. Desktop CLI overrides are separated from persisted app preferences, while one structured schema generates parser configuration, app-pref override membership, help text, and the exact guide block; package examples include the corrected PowerShell invocation. Focused direct final QA passed three suites with 63 passing tests and 2 pre-existing todos, both TypeScript configurations, full lint/documentation/formatting, the mdBook build with generated-link inspection, and diff checks. Independent repeat documentation review and QA completed with no actionable findings. Live target-native confirmation of default paths and packaged executable locations remains platform evidence, not a closure blocker.
- 2026-07-29 — Fixed DOC-004, DOC-005, and DOC-011 with conditional contributor browser bootstrap guidance, served-manual ownership and book-boundary validation, pinned mdBook CI builds, authoritative HTML resource checks, and removal plus reintroduction protection for the stale root workbench kit. Verification passed 16 focused checker/CI tests; the complete script suite passed 96 tests with one platform-specific skip; `check:docs` passed across 126 Markdown and 29 HTML files; and the pinned mdBook 0.5.4 build preserved both exact canonical repository links without escaping relative output. Full lint, formatting, and diff checks passed, and independent review plus QA completed with no findings. Playwright browsers were not installed or rerun in this batch; the audit's existing browser E2E evidence remains separate.
- 2026-07-29 — Fixed DOC-009 and DOC-012 with a real-binary developer Rust CLI help/version/dispatch contract, a complete ten-crate Rust workspace inventory and current integration documentation, checked production design-system imports, and reconciled tracked performance-reference provenance. ADR 0005 retains its accepted AppImage+ZIP/manual-update decision and adds a dated platform-specific executable-name clarification. Focused verification passed 6 CLI integration tests and 14 documentation-checker tests, Cargo formatting, `cargo test -p twine_cli --locked`, all-target `twine_cli` Clippy with warnings denied, `check:docs` across 126 Markdown and 29 HTML files, full lint, formatting, the pinned mdBook 0.5.4 build, and diff checks. Reviewer-driven correction made the July 16 50k reference explicitly historical rather than current/accepted; repeat independent review and QA completed with no findings. No live performance rerun was performed because the tracked normalized JSON references were the evidence source. Cross-platform developer CLI execution remains routine platform evidence, not a closure blocker.
- 2026-07-29 — Final complete-diff reconciliation corrected the two missing active crates in the workspace inventory, cross-platform executable naming in accepted ADR 0005, and two stale active-remediation workbench paths. Cumulative review found all three issues; repeat review completed with no findings. Broad cumulative QA preceded these final documentation-only corrections: 286 Jest suites reported 2,349 pass, 3 skip, and 17 todo; the Cargo workspace passed 262 unit/integration tests plus its doc-test groups, workspace Clippy and formatting, and the pinned mdBook build. Targeted repeat checks after the corrections passed the command-line Jest suite with 8 tests, the documentation checker with 15 tests, the complete script suite with 100 pass and 1 platform skip (101 total), both TypeScript configurations, `check:docs` across 126 Markdown and 29 HTML files, the official formatting policy, diff checks, and exact reconciliation of all 10 Cargo workspace members against the README. No new Playwright, performance, or target-native run was performed. The two remediation path cells preserve that file's pre-existing style: both `HEAD` and the current file fail standalone Prettier, while the official formatting policy excludes that directory and passes, so no unrelated broad reformat was taken.

## Scope limitations

- The repository had no `node_modules/` directory at audit start; the locked dependencies were installed for this audit.
- Current macOS ARM target-specific packaging and the earlier universal configuration were exercised locally. The quality and complete installable-artifact workflows are structurally covered by packaging tests, but Linux/Windows installers, install/uninstall flows, and non-host runtime behavior still require the new matrix's first CI execution.
- The broad packaged referenced-media scenario was not counted as a pass: its second format export timed out waiting for Electron's download event, while the two focused native/save packaged regressions passed. The timeout was not investigated as part of these release-input fixes.
- The signed profile's real Apple Developer ID/notarization path and Windows Authenticode path could not be executed because no identities or credentials were available. Their fail-closed configuration, simulated inspection outcomes, aggregate guards, and local macOS ad-hoc path are covered, but target-native signed artifacts still require credentialed release-run evidence. Generated local macOS artifacts are intentionally ad-hoc signed; publisher authenticity remains an accepted, documented residual risk only when `distributable-unsigned` is deliberately selected. The unsupported inherited device-usage descriptions were independently checked and are absent from the final plist.
- The permission policy and package declarations have unit and packaged-artifact coverage, but were not exercised against real camera, microphone, Bluetooth, HID, serial, or USB hardware.
- Reproducibility demonstrates byte-identical repeat builds in the pinned CI environment; it does not claim byte identity across operating systems.
- Dependency results reflect the npm advisory data available on 2026-07-22 and the recorded RustSec database revision.
- The new RustSec workflow was structurally validated but has not yet executed on GitHub; its exact strict audit policy passed locally against the cached database.
- Static review and targeted reproductions prioritize high-risk paths but cannot prove the absence of additional defects or vulnerabilities.
- The user-requested pause interval is excluded from active audit time.

## Excluded items

None. No file, snippet, section, or report item was skipped because of a content restriction.
